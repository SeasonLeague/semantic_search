{
  "buildCommand": "apt-get update && apt-get install -y tesseract-ocr && pip install -r requirements.txt && python -c \"import nltk; nltk.download('stopwords'); nltk.download('punkt'); nltk.download('wordnet')\"",
  "functions": {
    "api/python/**": {
      "runtime": "nodejs18.x"
    }
  }
}

